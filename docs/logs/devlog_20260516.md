# 作業ログ 2026-05-16

## 概要
有料カナリアテスト可否の実装監査を実施。3点の追加開発が必要と判定。
次回セッションで以下を**この順番**で修正していく。

---

## 判定結果

**カナリア可能（条件付き）** / 販売不可

- 課金導線E2E: ✅ 実装完了
- 無課金アクセス抜け道: ✅ 全経路ブロック
- 致命的未実装: ⚠️ 3件あり（後述）

---

## 次回作業：優先順位付きTODO

### 【作業1】週次メール自動送信のCron Job組込み（最優先）

**問題**:
- `magonotec-api/scripts/send_weekly_reports.py` は存在するが手動実行のみ
- `magonotec-api/render.yaml` には `web` サービスしか定義されていない
- 結果: **週次メールが自動で送られない**（販売前にブロッカー）

**修正内容**:
- `render.yaml` に Cron Job サービスを追加
- スケジュール: `0 0 * * 1`（毎週月曜 0:00 UTC = 9:00 JST）
- `startCommand: python scripts/send_weekly_reports.py`

**変更ファイル**: `magonotec-api/render.yaml`

**完了条件**:
- Render管理画面にCron Job サービスが表示される
- 手動trigger実行で実際にメールが届く
- `weekly_reports` テーブルに記録が増える

---

### 【作業2】解約時の迷子防止（403エラーハンドリング）

**問題**:
- `magonotec-app/js/main.js:1753` で 403 を汎用 `API error: 403` として throw
- フロントは「ごめんね。今は、うまくお返事ができなかったよ。」+ リトライボタンを表示
- 解約済ユーザーが**永遠にリトライ可能**で、なぜチャットできないか分からない

**修正内容**:
- `scheduleAiReply` のレスポンスチェックで `response.status === 403` を分岐
- 専用メッセージ: 「ご家族の登録が切れているみたい。ご家族に伝えてね」
- 既存の `showRegisterButton` フラグを再利用するか、新規 `showFamilyContact` フラグで誘導
- localStorageの `pairing_id` をクリアするか検討（→クリアすると登録画面に行ける）

**変更ファイル**: `magonotec-app/js/main.js`（scheduleAiReplyのエラー処理周り、1748-1855行付近）

**完了条件**:
- Stripe Test mode で解約 → 403が返る状況を作って実際に画面を確認
- 「ご家族に連絡してね」メッセージと登録ボタンが表示される

---

### 【作業3】RESEND_FROM_EMAIL を独自ドメインに切替

**問題**:
- 現状デフォルト `onboarding@resend.dev`（`magonotec-api/app/services/email_service.py:14`）
- Resend デフォルトドメインは spam フォルダ行き率が高い
- 家族にpairing URLが届かなければ全てが破綻

**修正内容**:
- Resendダッシュボードで独自ドメイン認証（SPF/DKIM）
- Render環境変数 `RESEND_FROM_EMAIL` を `noreply@<独自ドメイン>` などに設定
- ドメインを持っていなければ取得（お名前.com等）から

**完了条件**:
- Resendダッシュボードでドメイン verified
- テスト送信が受信トレイ（spamフォルダではない）に届く

---

## カナリア出発前チェックリスト（作業前に確認）

Render管理画面で以下5つの環境変数が**実値で**入っているか目視確認:

- [ ] `DATABASE_URL` (PostgreSQL接続文字列)
- [ ] `STRIPE_SECRET_KEY` (sk_test_... または sk_live_...)
- [ ] `STRIPE_WEBHOOK_SECRET` (whsec_...)
- [ ] `STRIPE_PRICE_ID` (`price_1SjQsIGz8VV9feZNJ6xjWCfk`)
- [ ] `RESEND_API_KEY` (re_...)

Stripeダッシュボード側:

- [ ] Webhook endpoint URL登録: `https://magonotec-api.onrender.com/api/stripe/webhook`
- [ ] イベント `checkout.session.completed` 有効
- [ ] イベント `customer.subscription.deleted` 有効
- [ ] イベント `customer.subscription.updated` 有効

E2E確認:

- [ ] Stripe Test mode で card `4242 4242 4242 4242` で実E2E 1周
- [ ] `subscriptions` テーブルに新規レコード作成確認
- [ ] Renderログに `[INFO] New subscription created` 出力確認
- [ ] pairing URL メールが届く（Resend設定済みなら）
- [ ] 親URLでチャット可能

---

## 監査で確認済みの「問題なし」項目（再確認不要）

- ✅ `/api/chat` の pairing_id 検証（400/403、UUID形式チェック、active検証）
- ✅ `/api/usage/record` の pairing_id 検証（同上）
- ✅ 画像送信・音声入力も同じ `/api/chat` ルート経由なのでブロック有効
- ✅ Stripe Webhook の冪等化（`stripe_subscription_id` 重複チェック）
- ✅ `customer.subscription.deleted` で `status=canceled` 更新
- ✅ `customer.subscription.updated` で `past_due` を非active化
- ✅ 利用日数記録の1日1回制約（`uq_usage_day` unique constraint）
- ✅ 個人情報非共有: 会話履歴はlocalStorageのみ、DB保存なし、家族メールは日数のみ
- ✅ CORS設定: `allow_credentials=True` + 具体的なorigins
- ✅ `email-validator` 依存追加済（requirements.txt）

---

## 関連ファイル早見表

### バックエンド（C:\Users\yukih\magonotec-api）
- `app/main.py` - /api/chat、pairing_id検証
- `app/routers/billing.py` - Stripe Checkout Session作成
- `app/routers/stripe_webhook.py` - Webhook受信、subscription状態管理
- `app/routers/usage.py` - 利用日記録
- `app/services/email_service.py` - Resendメール送信
- `app/models.py` - Subscription, UsageDay, WeeklyReport
- `app/schemas.py` - Pydanticスキーマ
- `scripts/send_weekly_reports.py` - 週次レポート送信（Cron用）
- `render.yaml` - **作業1で要編集**

### フロントエンド（C:\Users\yukih\magonotec-app）
- `js/main.js` - メインロジック（2137行）、**作業2で要編集**
- `register.html` - 家族登録ページ
- `register_success.html` - 登録完了・親URL発行
- `index.html` - チャット画面

---

## 作業1 実施記録（2026-05-18）

### 変更内容
- `magonotec-api/render.yaml` に Cron Job サービス `magonotec-weekly-reports` を追加
  - `schedule: "0 0 * * 1"`（毎週月曜 0:00 UTC = 9:00 JST）
  - `startCommand: python scripts/send_weekly_reports.py`
  - 環境変数 `DATABASE_URL` / `RESEND_API_KEY` / `RESEND_FROM_EMAIL` を `sync: false` で宣言（Render管理画面で実値を投入）
- `scripts/send_weekly_reports.py` は無変更（既存仕様で十分）
  - 本文: `今週は {N}日、まごのTECを使われていました。` のみ
  - 評価・警告文言なし
  - `weekly_reports` テーブルへの重複防止チェック（pairing_id + week_start）維持

### Render管理画面で必要な作業（デプロイ後）
1. 次回デプロイ時に Render が `render.yaml` を読んで Cron Job を自動作成
2. 作成された Cron サービスの環境変数欄で以下を設定：
   - `DATABASE_URL`（web サービスと同じ値）
   - `RESEND_API_KEY`
   - `RESEND_FROM_EMAIL`（作業3で独自ドメインに切替予定）
3. 手動 trigger で1回テスト実行 → メール受信と `weekly_reports` レコード作成を確認

### 補足（2026-05-19 追記）：実際の Render 運用判明事項
- `magonotec-api` は **Blueprint未使用**（手動作成）と確定。Blueprints画面に登録なし
- → `render.yaml` の push だけでは Cron Job は作られない
- → Render ダッシュボードで **+ New → Cron Job** から手動作成して対応
- `render.yaml` はリポジトリのソース・オブ・トゥルースとして残置（push してOK、Render側は無視）

### 動作確認結果（2026-05-19 手動 Trigger Run）

実行ログ:
```
==> Cron job run started
==> Running 'python scripts/send_weekly_reports.py'
[INFO] Sending weekly reports for 2026-05-11 - 2026-05-17
[INFO] Found 1 active subscriptions
[INFO] Report already sent for e490975f-5de6-4cd1-979e-aab1121ae4f0
[INFO] Weekly reports completed
==> Cron job run finished successfully
```

検証済み項目:
- ✅ Cron Job のスケジュール起動（手動trigger経由、実体は同じ）
- ✅ DB接続（DATABASE_URL正しい、active subscription 1件発見）
- ✅ 先週期間算出ロジック（5/11月〜5/17日）
- ✅ weekly_reports 重複防止チェック動作（既存記録に対する二重送信回避）
- ✅ 例外なし正常終了（exit code 0）

未検証だが想定通り:
- ⏸️ 実メール送信パス（既存テスト送信記録の存在により今回スキップ）
- → 次回scheduled実行（2026-05-25月曜 9:00 JST、5/18-5/24週分）で自然に検証される

**追記（2026-05-19）**: Resend ダッシュボードで過去の送信実体を目視確認済み → 作業1 **完全完了**。

### 残作業
- 作業2: 解約時の403エラーUX（`magonotec-app/js/main.js`）
- 作業3: RESEND_FROM_EMAIL 独自ドメイン切替

---

## 作業2 実施記録（2026-05-18）

### 変更内容
- `magonotec-app/js/main.js` の `scheduleAiReply` を編集
- レスポンスチェック箇所（1748行付近）で `response.status === 400 || response.status === 403` を検出し `throw new Error('UNREGISTERED')`
- catch ブロックに `UNREGISTERED` 分岐を追加
  - 固定メッセージ: 「ご家族の登録が確認できなかったよ。\n\nわからなければ、ご家族に聞いてみてね。」
  - `showRegisterButton: true`（既存の登録ボタン仕組みを再利用）
  - `showRetry: false`（リトライボタンを出さない）
- `fallbackMessage` オブジェクトに `showRegisterButton` フィールドを追加

### 設計判断
- 「解約」「支払い失敗」「期限切れ」といった直接表現は一切使わない（指示書の制約）
- 400 と 403 を同じ「未登録」導線で扱う
  - 400: pairing_id 欠如・形式不正
  - 403: 有効な subscription なし（解約・支払い失敗・未登録の全パターンが該当）
- 既存の `showUnregisteredMessage()` が使う `showRegisterButton` フラグと `register-button` クラスをそのまま流用 → renderMessages 側の変更不要
- localStorage の `pairing_id` はクリアしない
  - 理由：何度試しても同じやさしいメッセージが出るだけで、無限リトライ地獄にはならない
  - ユーザーが「登録ページを開く」ボタンを能動的に押すと register.html で再登録できる
- 500 など一時障害は既存の汎用エラー＋リトライ動作を維持

### 動作確認方法（テスト時）
1. Stripe Test mode で active な subscription を作成 → 親URLでチャット可能を確認
2. Stripe ダッシュボードで該当 subscription を cancel
3. 親URLで再度チャット送信 → 「ご家族の登録が確認できなかったよ」と登録ボタン表示
4. リトライボタンが**出ない**ことを確認
5. localStorage を手動でクリア → 同じ画面で再送信 → 同じメッセージ表示（400経由）

### 残作業
- 作業3: RESEND_FROM_EMAIL 独自ドメイン切替

---

## 作業3 実施記録（2026-05-18）

### コード変更の要否：**コード変更なし**

確認結果（grep 全件チェック済み）:
- `app/services/email_service.py:14` — `os.environ.get("RESEND_FROM_EMAIL", "onboarding@resend.dev")` で環境変数から読込
- `app/services/email_service.py:63, 97` — `from` は `f"まごのTEC <{RESEND_FROM_EMAIL}>"` で動的構築
- `scripts/send_weekly_reports.py:31, 97` — 同様に環境変数から読込・動的構築
- ハードコードされた送信元アドレスはコード内に**一切なし**
- デフォルト値 `onboarding@resend.dev` は開発用フォールバック。Render 環境変数で上書きされるため運用上は問題なし
- `.env.example:36` にも `RESEND_FROM_EMAIL=onboarding@resend.dev` と例示済み（こちらも本番では上書きされる）

→ Resend ドメイン認証と Render 環境変数更新だけで切替完了する設計になっている。

---

### 運用手順（次回セッションで実施）

#### Step 1. ドメイン取得（未取得の場合）
- お名前.com / Cloudflare Registrar / Google Domains いずれかで取得
- 例：`magonotec.com` `magonotec.app` `magonotec.jp` など
- 送信専用ならサブドメインでもOK（例：`mail.magonotec.com`）

#### Step 2. Resend 側の作業
1. Resend ダッシュボード → **Domains** → **Add Domain**
2. 取得した独自ドメインを入力
3. Resend が以下のDNSレコードを表示するので、ドメインのDNS管理画面に追加：
   - **SPF（TXT）**: `v=spf1 include:amazonses.com ~all`
     （Resend は AWS SES バックエンド。Resend ダッシュボードに表示される値をそのまま使う）
   - **DKIM（CNAME × 3 または TXT）**: Resend が発行する `resend._domainkey...` 系レコード
   - **MX（任意）**: Resend が指定する場合のみ
   - **Return-Path（CNAME、推奨）**: 認証強化用
4. DNS反映を待つ（数分〜最大48時間、通常は15分以内）
5. Resend ダッシュボードで **Verified** バッジが付いたら認証完了

#### Step 3. Render 環境変数の更新
1. Render ダッシュボード → `magonotec-api`（webサービス） → **Environment**
2. `RESEND_FROM_EMAIL` を更新：
   - 旧: `onboarding@resend.dev`（または未設定）
   - 新: `noreply@<取得した独自ドメイン>`（例：`noreply@magonotec.com`）
3. **重要**：作業1で追加した Cron Job `magonotec-weekly-reports` の環境変数にも同じ値を設定
   - web と cron で別サービスのため、両方更新が必要
4. Render が自動で web サービスを再デプロイ。Cron Job は次回スケジュール時に新環境変数を反映

#### Step 4. テスト
1. **pairing URL 送信テスト**: 新規 Stripe Test 課金 → `subscriptions.family_email` に届くか確認
   - 受信トレイ（spam ではなく）に届くこと
   - From 表示が「まごのTEC <noreply@独自ドメイン>」になっていること
2. **週次レポート手動実行テスト**: Render ダッシュボード上で Cron Job を手動 trigger
   - メール受信を確認
   - 同じく受信トレイ着・From 表示自然を確認
3. Gmail / Yahoo / docomo / au / SoftBank の各キャリアで spam フォルダ判定を最低1件ずつ確認推奨

---

### チェックポイント
- [ ] 独自ドメイン取得済み
- [ ] Resend にドメイン追加
- [ ] SPF / DKIM レコードを DNS に登録
- [ ] Resend で Verified 確認
- [ ] Render `magonotec-api` の `RESEND_FROM_EMAIL` 更新
- [ ] Render `magonotec-weekly-reports`（Cron）の `RESEND_FROM_EMAIL` 更新
- [ ] pairing URL メールが受信トレイに届く
- [ ] 週次レポートメールが受信トレイに届く（手動trigger）
- [ ] From 表示が自然

---

## 3作業完了サマリー

| 作業 | 状態 | コード変更 | 残作業 |
|---|---|---|---|
| 1. 週次メールCron Job | ✅ **完了** | `render.yaml` 編集 + Render手動Cron作成 | 実メール送信は次回scheduled実行（5/25）で自然検証 |
| 2. 解約時403 UX | ✅ コード反映済 | `js/main.js` 編集 | Stripe Test mode で実機確認 |
| 3. RESEND独自ドメイン | ⏸️ 運用待ち | **コード変更なし** | ドメイン取得 → Resend認証 → Render env vars 更新 |

カナリア出発条件は「作業3 完了 + カナリア出発前チェックリスト全項目クリア」になる。

---

## 復活の呪文（次回セッション開始時にコピペ）

```
まごのTECの作業を再開します。

【現状】
有料カナリアテスト出発前。3点の追加開発が必要と判定済み。
docs/logs/devlog_20260516.md を読んで、TODOの順番通りに作業してください。

【優先順位】
1. 週次メールCron Job組込み（render.yaml編集）
2. 解約時の403エラーUX（main.js編集）
3. RESEND_FROM_EMAIL独自ドメイン切替

【判定済みOK事項】
課金導線、無課金ブロック、解約status更新、個人情報非共有、利用日数記録は実装済み。
監査で確認済みなので再調査不要。

【ローカルパス】
- フロント: C:\Users\yukih\magonotec-app
- バック: C:\Users\yukih\magonotec-api
```
